# Existing Vendor Lookup Policy

Before creating a new vendor record, the system should determine whether the
vendor already exists.

If an existing vendor record is found, the reviewer should verify:

- Vendor status
- Previous onboarding date
- Existing certifications
- Prior compliance findings

Existing vendors with valid documentation may reuse documents that have not
expired.

If documentation has expired, updated copies must be provided before
approval.